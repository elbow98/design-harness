# Design.md — 디자인 시스템 + 철학

> 버전: v0 (2026-09-23) · 원본 토큰: `vault/tokens.css` · 카탈로그: `vault/components.html` · 차트: `vault/charts.html`
> 이 파일은 **무엇이 어떻게 보이는지**를 정한다. 여기 없는 값은 쓰지 않는다 (Rules.md §5-1).
> v0는 특정 제품이 아닌 **개인 플레이그라운드의 중립 기본값**이다. 세션마다 교정을 받아 내 취향으로 굳혀 간다.

---

## 1. 철학

1. **내용이 먼저, 장식은 나중.** 화면의 주인공은 데이터와 문장이다. 장식이 내용과 시선을 다투면 장식을 뺀다.
2. **색은 하나만 말한다.** 기본은 무채색, 테마색은 **버건디**(고급·편집 매체 느낌). 강조색(accent)은 "여기를 누르세요/여기가 지금 상태입니다"에만 쓴다. 한 화면에 accent가 쓰인 곳이 3군데를 넘으면 의심한다.
3. **위계는 크기·굵기·간격으로.** 박스·선·그림자로 위계를 만들기 전에 여백과 타입 스케일로 먼저 해결한다.
4. **밀도는 정직하게.** 도구 화면은 촘촘해도 된다. 여백을 늘려 비어 보이게 하지 않고, 욱여넣어 답답하게 하지도 않는다.
5. **실제처럼.** 실제 한국어 문구, 현실적인 숫자, 실제로 생기는 상태(빈 목록, 에러, 긴 이름)를 처음부터 넣는다.
6. **한국어를 기준으로.** 한글은 영문보다 글자 면적이 크다. 행간·자간·줄바꿈(`word-break: keep-all`)을 한글 기준으로 잡는다.

## 2. 색

모든 색은 CSS 변수로만 쓴다. 라이트/다크 두 벌이 항상 짝으로 정의된다.

| 토큰 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| `--bg` | `#FAFAF9` | `#111110` | 페이지 바탕 |
| `--surface` | `#FFFFFF` | `#1A1A19` | 떠 있는 면 (입력창, 패널) |
| `--surface-2` | `#F3F3F1` | `#232321` | 눌린 면, hover 배경, 코드 블록 |
| `--border` | `#E6E5E1` | `#2E2E2B` | 기본 구분선 |
| `--border-strong` | `#CFCDC7` | `#474641` | 입력창 테두리, 강한 구분 |
| `--text` | `#1A1A19` | `#EDEDEA` | 본문, 제목 |
| `--text-2` | `#5C5B57` | `#A6A59F` | 보조 설명 |
| `--text-3` | `#8A8883` | `#77766F` | 메타 정보, placeholder |
| `--accent` | `#9B2C4E` | `#EE8AAA` | 주 행동, 링크, 선택 상태 |
| `--accent-hover` | `#7E223F` | `#F4A7BF` | accent hover |
| `--accent-soft` | `#F7E8ED` | `#3A1A25` | 선택된 행, 강조 배경 |
| `--on-accent` | `#FFFFFF` | `#1F0A11` | accent 위 글자 |
| `--positive` | `#1D7F46` | `#4CC07E` | 성공, 좋은 변화 |
| `--negative` | `#CC3333` | `#F07070` | 에러, 삭제, 나쁜 변화 |
| `--warning` | `#9A6512` | `#E2A847` | 주의 |
| `--up` | `#D42A2A` | `#FF5A5F` | **시장 상승** (가격·수익률) |
| `--down` | `#1B64DA` | `#5B9BFF` | **시장 하락** (가격·수익률) |
| `--focus` | `#9B2C4E` | `#EE8AAA` | 포커스 링 |

규칙
- 상태색(positive/negative/warning)은 **글자·아이콘·얇은 선**에 쓴다. 넓은 면을 칠하지 않는다.
- 방향은 색만으로 구분하지 않는다: `▲ +2.4%` / `▼ −1.1%`처럼 기호와 부호를 같이 쓴다.
- **시장 데이터(가격·지수·수익률·환율)는 한국식: 색 = 방향.** 상승 `--up`(빨강), 하락 `--down`(파랑), 보합은 `--text-2`. 클래스 `.up` / `.down`.
- **그 외 지표는 색 = 좋고 나쁨, 화살표 = 방향.** 둘을 섞지 않는다. 예: 소요 시간 감소는 `▼ −6분`(`.good`), 에러 증가는 `▲ +2`(`.bad`).
- 한 화면에 두 규칙이 같이 나오면 시장 데이터 쪽이 빨강/파랑, 일반 지표 쪽이 초록/빨강이 되어 헷갈릴 수 있다 — 일반 지표는 가능하면 색 없이 부호만 쓴다.
- 본문 대비 4.5:1 이상. `--text-3`은 12px 이하 본문에 쓰지 않는다.

## 3. 타이포그래피

- 본문: **Pretendard Variable** (jsDelivr 동적 서브셋) → `-apple-system, "Apple SD Gothic Neo", system-ui, sans-serif`
- 숫자·코드: `ui-monospace, "SF Mono", Menlo, monospace`
- 숫자가 정렬돼야 하는 곳(표, 지표)은 `font-variant-numeric: tabular-nums`.

| 토큰 | 크기 / 행간 | 굵기 | 용도 |
|---|---|---|---|
| `--fs-display` | 44 / 1.15 | 700 | 랜딩 헤드라인 (화면당 1개) |
| `--fs-h1` | 32 / 1.25 | 700 | 페이지 제목 |
| `--fs-h2` | 24 / 1.3 | 650 | 섹션 제목 |
| `--fs-h3` | 19 / 1.4 | 600 | 카드·그룹 제목 |
| `--fs-body` | 16 / 1.65 | 400 | 본문 |
| `--fs-sm` | 14 / 1.55 | 400 | UI 텍스트, 표 |
| `--fs-xs` | 12 / 1.5 | 500 | 라벨, 캡션, 배지 |

규칙
- 제목 자간은 `-0.02em`, 본문은 `0`. 한글 본문에 음수 자간을 주지 않는다.
- 굵기는 표에 있는 값(400 / 500 / 600 / 650 / 700)만 쓴다. 500은 라벨·배지 전용.
- 본문 한 줄은 최대 `68ch`.
- 모바일(≤640px)에서 display 32, h1 26, h2 21로 줄인다.

## 4. 간격·레이아웃

4px 기반. 토큰 외의 간격은 쓰지 않는다.

| 토큰 | 값 | | 토큰 | 값 |
|---|---|---|---|---|
| `--sp-1` | 4px | | `--sp-6` | 24px |
| `--sp-2` | 8px | | `--sp-8` | 32px |
| `--sp-3` | 12px | | `--sp-10` | 40px |
| `--sp-4` | 16px | | `--sp-14` | 56px |
| `--sp-5` | 20px | | `--sp-20` | 80px |

- 컨테이너 최대 폭 `--container: 1120px`, 좌우 여백 모바일 16px / 데스크톱 24px.
- 관련된 것은 가깝게(`--sp-2`~`--sp-3`), 다른 그룹은 멀게(`--sp-8` 이상). 중간값으로 뭉개지 않는다.
- 브레이크포인트: `640px`(모바일↔태블릿), `1024px`(태블릿↔데스크톱). 가로 스크롤은 표 내부에서만 허용.

## 5. 모양·깊이·움직임

| 토큰 | 값 | 용도 |
|---|---|---|
| `--r-sm` | 6px | 배지, 체크박스 |
| `--r-md` | 8px | 버튼, 입력창 |
| `--r-lg` | 12px | 패널, 모달 |
| `--r-full` | 999px | 칩, 아바타 |
| `--shadow-pop` | `0 8px 24px rgb(0 0 0 / .12), 0 1px 2px rgb(0 0 0 / .08)` | 떠 있는 것만 (메뉴, 모달, 토스트) |
| `--dur-fast` | 120ms | hover, 눌림 |
| `--dur-base` | 200ms | 열림/닫힘 |
| `--ease` | `cubic-bezier(.2, 0, 0, 1)` | 모든 전환 |

- 그림자는 **떠 있는 레이어에만.** 페이지에 붙어 있는 카드는 `1px solid var(--border)`로 충분하다.
- `prefers-reduced-motion: reduce`면 전환을 끈다.

## 6. 컴포넌트

실물은 `vault/components.html`. 새 컴포넌트는 거기서 포크한다.

| 컴포넌트 | 규격 |
|---|---|
| **Button** | 높이 40(기본)/32(sm). `primary`(accent 채움) · `secondary`(surface + border-strong) · `ghost`(배경 없음) · `danger`(negative 글자). 화면당 primary는 1개. 아이콘만 있는 버튼은 `aria-label` 필수 |
| **Input / Select** | 높이 40, `--r-md`, border-strong. 라벨은 위(`--fs-xs`, text-2), 에러 메시지는 아래(negative, `--fs-xs`). placeholder를 라벨 대신 쓰지 않는다 |
| **Badge** | `--fs-xs`, 높이 22, surface-2 배경, 줄바꿈 금지. 상태 배지는 점(●) + 글자 |
| **Table** | 행 높이 44, 헤더는 `--fs-xs` text-2 대문자 없음. 숫자 열 우측 정렬 + tabular-nums. 세로선 없음, 가로선만 |
| **Tabs** | 밑줄형. 선택 탭은 text + 2px accent 밑줄 |
| **Panel** | surface + border + `--r-lg`, 안쪽 여백 `--sp-6`. 패널 안에 패널을 넣지 않는다 |
| **Stat** | 라벨(`--fs-xs` text-2) → 값(`--fs-h2`, tabular-nums) → 변화량(▲▼ + 부호 + 색: 시장 데이터는 `.up`/`.down`, 그 외는 `.good`/`.bad`) |
| **Empty state** | 한 줄 설명 + 다음 행동 버튼 1개. 일러스트 없음 |
| **Toast** | 화면 하단 중앙, surface + `--shadow-pop`, 4초 후 사라짐, 닫기 버튼 |

아이콘: [Lucide](https://lucide.dev) 스타일의 1.5px 선 아이콘, 16/20px. 이모지를 아이콘으로 쓰지 않는다.

## 7. 인터랙션 체크리스트

프로토타입마다 해당되는 항목을 확인한다.

- [ ] **포커스**: Tab 키로 모든 조작 요소에 도달, `:focus-visible` 링 2px `--focus` + 2px offset
- [ ] **상태**: hover / active / disabled / selected 가 서로 구분됨
- [ ] **빈 상태**: 데이터가 0개일 때 화면
- [ ] **에러 상태**: 입력 오류, 네트워크 실패 문구
- [ ] **로딩**: 300ms 넘으면 스켈레톤 또는 스피너
- [ ] **overflow**: 긴 한국어 이름, 긴 숫자, 줄바꿈되는 버튼 라벨
- [ ] **반응형**: 390px에서 가로 스크롤 없음, 터치 타깃 40px 이상
- [ ] **다크 모드**: `prefers-color-scheme`와 `data-theme` 토글 둘 다
- [ ] **새로고침**: 상태를 유지하는지(localStorage) 안 하는지 명시

## 8. 하지 않는 것 (anti-slop)

에이전트가 기본값으로 내기 쉬운 것들. 사용자가 명시적으로 요청할 때만 쓴다.

- 보라·핑크 그라디언트 배경, 그라디언트 글자
- 모든 요소를 둥근 카드 + 그림자로 감싸기, 카드 안의 카드
- 이모지 아이콘 (🚀 ✨ 📊), 의미 없는 장식 일러스트
- 글래스모피즘, 네온 글로우, 과한 blur
- "Unlock your potential" 류의 빈 마케팅 문구, Lorem ipsum
- 근거 없는 가짜 지표 (+312%, 10x) 
- 가운데 정렬된 긴 본문, 회색 위의 회색 글자
- 모든 섹션에 같은 3열 feature 그리드
- 화면마다 다른 radius·그림자·간격

거절된 실제 사례는 `vault/rejected/`에 쌓인다. 규칙이 헷갈리면 거기서 이유를 확인한다.

## 9. 차트

실물은 `vault/charts.html`. 모든 값은 dataviz 검증기(`validate_palette.js`)로 검증했다. 눈대중으로 색을 바꾸지 않는다 — 바꾸면 검증기를 다시 돌린다.

### 먼저 형태를 고른다 (색은 마지막)
- 숫자 하나가 핵심이면 차트가 아니라 **Stat**(§6)이다. 막대 1개짜리 차트, 조각 2개짜리 파이 금지.
- 추이 = 선, 크기 비교 = 막대, 구성비 = 누적 막대(파이는 6조각 이하일 때만), 두 변수 관계 = 산점도, 값의 격자 = 히트맵.
- **y축은 하나.** 단위가 다른 두 지표는 차트 두 개로 나누거나 기준시점 = 100으로 지수화한다.

### 색의 네 가지 역할
| 역할 | 토큰 | 규칙 |
|---|---|---|
| **범주** (어느 계열인가) | `--series-1` ~ `--series-8` | **순서대로 배정, 순환 금지.** 9번째 계열은 “기타”로 묶거나 작은 차트 여러 개로 나눈다. 필터로 계열이 빠져도 남은 계열의 색은 그대로 (색은 순위가 아니라 대상을 따른다) |
| **순차** (얼마나 큰가) | `--seq-1`(작음) ~ `--seq-9`(큼) | 버건디 한 색상. 다크에서는 토큰이 알아서 뒤집힌다(작은 값 = 배경에 가까운 어두운 단계). 순서 있는 구간(단계·등급)에 쓸 땐 라이트는 `--seq-3`부터, 다크는 `--seq-3`부터 |
| **발산** (기준보다 위냐 아래냐) | `--div-neg-3…1` · `--div-mid` · `--div-pos-1…3` | 파랑(음) ↔ 회색 중립 ↔ 버건디(양). 가운데는 항상 회색 |
| **상태** (좋다/나쁘다) | `--positive` `--negative` `--warning` | 계열 색으로 쓰지 않는다. 항상 ▲▼·아이콘 + 글자와 함께 |
| **시장 방향** (올랐나/내렸나) | `--up` `--down` | 등락 표시(캔들·등락 막대·등락률 글자) 전용. 계열 색으로 쓰지 않는다 |

| 슬롯 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| 색 | 버건디 | 골드 | 블루 | 오렌지 | 아쿠아 | 바이올렛 | 그린 | 마젠타 |
| 라이트 | `#9B2C4E` | `#EDA100` | `#2A78D6` | `#EB6834` | `#1BAF7A` | `#4A3AA7` | `#008300` | `#E87BA4` |
| 다크 | `#B03A62` | `#C98500` | `#3987E5` | `#D95926` | `#199E70` | `#9085E9` | `#008300` | `#D55181` |

검증 결과 (배경 라이트 `#FFFFFF` / 다크 `#1A1A19`)
- 인접 쌍 색각이상 구분 ΔE: 최저 9.2 / 9.4 (목표 8 이상) · 정상 시각 ΔE: 최저 27.6 / 22.2 (하한 15 이상)
- 슬롯 1–3은 **모든 쌍** 검증도 통과 → 산점도·버블·지도·작은 차트 여러 개는 **계열 3개까지**, 넘으면 “기타”로 묶거나 나눈다
- 라이트에서 골드·아쿠아·마젠타는 배경 대비 3:1 미만 → 이 색을 쓰는 차트는 **직접 라벨 또는 표 보기 필수**
- 발산·순차 램프: 밝기 단조 증가·단계 간격 통과 (중립 쪽 끝이 배경에 스며드는 건 의도)

### 표시 규칙
- 선 2px(둥근 끝), 점 지름 8px 이상 + 배경색 2px 테두리, 막대 두께 24px 이하·끝 4px 둥글게(기준선 쪽은 각지게), 맞닿는 채움 사이는 배경색 2px 틈.
- 격자·축은 `--chart-grid`/`--chart-axis` 1px 실선. 점선 금지. 틈은 `--chart-gap`(2px), 범례 키 모서리는 `--chart-key-r`.
- 계열 2개 이상이면 **범례 필수**, 4개 이하면 선 끝에 직접 라벨도. 모든 점에 숫자 달지 않는다 — 끝값·최댓값·이야기의 주인공만.
- **글자는 데이터 색을 입지 않는다.** 라벨·값·범례 글자는 `--text`/`--text-2`/`--text-3`, 색은 옆의 점·선 키가 담당.
- 호버: 선·영역 차트는 세로 기준선 + 그 시점 모든 계열 값 툴팁, 막대·점·셀은 각자 툴팁. 툴팁은 보조일 뿐 — 같은 값을 **표 보기**로도 볼 수 있어야 한다.
- 상승·하락은 **한국식**(2026-09-23 결정). 발산 램프도 같은 방향: 양(+) = 버건디(빨강 계열), 음(−) = 파랑. 수익률 히트맵은 발산 램프, 등락 글자·캔들은 `--up`/`--down`.

## 10. 변경 이력

| 날짜 | 변경 | 근거 |
|---|---|---|
| 2026-09-23 | v0 생성 — 중립 기본값 | 개인 플레이그라운드로 시작, 교정으로 조정 예정 |
| 2026-09-23 | 테마색 코발트 → **버건디** (`#9B2C4E` / 다크 `#EE8AAA`) | 사용자 선택. 6개 후보 비교: `outputs/2026-09-23-theme-color-explore` · 대비 라이트 7.0:1, 다크 8.0:1 |
| 2026-09-23 | 차트 팔레트 추가 (§9): 범주 8색·순차·발산, 검증기 통과 | 사용자 요청 · 버건디를 1번 슬롯에 두고 나머지 순서는 7! 전수 탐색으로 결정 |
| 2026-09-23 | 상승·하락 **한국식** 채택: `--up` 빨강 / `--down` 파랑 신설, `--positive`/`--negative`는 성공·에러 전용으로 분리 | 사용자 결정 · 빨강–초록은 색각이상 구분이 가장 어렵고, 발산 램프(버건디↔파랑)와 방향이 일치 · 대비 라이트 4.8/5.2, 다크 6.2/6.8 |
